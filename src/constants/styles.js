import { StyleSheet } from 'react-native';
import { colors, spacing } from './theme';

export const cakrokStyle = StyleSheet.create({
    safeArea:{
        flex: 1,
        backgroundColor:'#f3f3f3',
        padding:'20'
    }
});
